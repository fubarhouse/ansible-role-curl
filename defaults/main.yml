---
# defaults file for ansible-role-curl

curl_buildfromsource: false
curl_version: "7.54.1"
curl_path: "/tmp"
curl_extension: "tar.gz"
curl_source: http://curl.haxx.se/download
curl_configure_flags:
  - disable-shared
  - with-ssl
